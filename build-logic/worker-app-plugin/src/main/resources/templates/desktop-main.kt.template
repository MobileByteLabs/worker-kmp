package {{packageName}}.generated

import io.github.mobilebytelabs.worker.desktop.launchDesktopWorkerApp
import {{koinFnFqn}}
import {{contentFnFqn}}

/** Auto-generated by io.github.mobilebytelabs.worker-app — DO NOT EDIT.
 *
 *  worker-kmp v4.0.0 — `appKoinModules()` no-arg signature (D24). The
 *  `launchDesktopWorkerApp` wrapper internally calls `WorkerKmpAuto.install()` after
 *  `startKoin { modules(koinModules) }` returns — see launchDesktopWorkerApp's onSetup hook.
 */
public fun main(): Unit = launchDesktopWorkerApp(
    title = "{{title}}",
    koinModules = { {{koinFnSimpleName}}() },
    onAfterKoinStart = { WorkerKmpAuto.install() },
) { {{contentFnSimpleName}}() }
