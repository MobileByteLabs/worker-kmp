package {{packageName}}.generated

import io.github.mobilebytelabs.worker.android.WorkerKmpStarterApplication
import {{koinFnFqn}}

/** Auto-generated by io.github.mobilebytelabs.worker-app — DO NOT EDIT.
 *
 *  worker-kmp v4.0.0 — single-API completion:
 *   - `appKoinModules()` signature dropped the `WorkManagerFactory` parameter (D24).
 *   - WorkerKmpAuto.install() (codegen-emitted commonMain shim) replaces the legacy
 *     `workKoinModule(WorkerConfig(), workers, androidWorkManagerFactory(this))` call —
 *     it reads `Application` from Koin's `androidContext()` binding, selects the factory,
 *     builds the worker registry from @WorkerKmpWorkers annotation sites, and calls
 *     `WorkerKmpHost.initialize`.
 */
public class Generated_App : WorkerKmpStarterApplication() {
    override fun koinModules() = {{koinFnSimpleName}}()

    override fun onCreate() {
        super.onCreate()
        WorkerKmpAuto.install()
    }
}
