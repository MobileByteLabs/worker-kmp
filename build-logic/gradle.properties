# Mirror of root worker.version — build-logic is an includedBuild so it doesn't
# inherit the parent's gradle.properties. Source of truth: ../gradle.properties.
# Bump both in lockstep; lib-release verifies they match before publishing.
worker.version=2.1.2
