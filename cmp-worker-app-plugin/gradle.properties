# Mirror of root worker.version — kept in sync because cmp-worker-app-plugin
# is an included build (via pluginManagement.includeBuild) and Gradle does NOT
# inherit gradle.properties from the parent root.
#
# Source of truth: ../gradle.properties — bump both in lockstep. CI gate:
# the lib-release script verifies these match before publishing.
worker.version=2.1.2

# Gradle daemon / JVM args inherited via system default (no overrides needed
# at the plugin sub-build — main build sets them).
